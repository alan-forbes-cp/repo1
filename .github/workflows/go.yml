name: go
on:
  workflow_dispatch:
jobs:
  gojob1:
    runs-on: ubuntu-22.04
    steps:
    - run: which ccache || echo no ccache
    - run: which ccache -V || echo no version
  gojob2:
    runs-on: ubuntu-24.04
    steps:
    - run: which ccache || echo no ccache
    - run: which ccache -V || echo no version
